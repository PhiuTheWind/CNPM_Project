const express = require('express');
const router = express.Router();
const spsoController = require('../controllers/spsoController');

// route.post('/add_printer', spsoController.add_printer);
// route.post('/modify_status', spsoController.modify_status);
// route.get('/report', spsoController.report);

module.exports = router;    